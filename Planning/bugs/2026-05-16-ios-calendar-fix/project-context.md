# Project Context — bugfix-2026-05-16-ios-calendar-fix

_Generated by bugfix-tomi priming sub-agent, 2026-05-16._

## Project slug
`kalori-mvp` (root `package.json` name: `kalori`, version `0.1.0`, private).

## Tech stack
- **Language:** TypeScript strict (`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`).
- **Framework:** Next.js 16.2.4 App Router + React 19.2.5; route groups `(app)`, `(auth)`, `(marketing)`; RSC-first / streaming islands; no root `middleware.ts` — auth via `requireProfileOrRedirect` in `lib/auth/orphan-profile-fence.ts`.
- **UI:** Tailwind v4.2.2 + shadcn/ui + Radix + framer-motion 12.38; design = "The Ledger" (dark oxblood/ivory, Newsreader serif + Inter + JetBrains Mono, zero-radius, hairlines).
- **State / data:** Zustand 5; Supabase (`@supabase/ssr 0.10.2`); Gemini `gemini-flash-latest`.
- **PWA:** `@serwist/next 9.5.7` + custom SW.
- **Test runner:** Vitest 4.1.4 (threads, single-worker) + Playwright 1.59 + axe + MSW 2.13.
- **Infra:** Vercel + Sentry (errors-only); pnpm 10.29.3; Node ≥20.19.0 (CI Node 24 floor).

## Recent work direction (last 5 commits)
17a13b3, a2d6f7b, 5e7165f, 9988ffc, c4dd4a8 — all Phase D of the `mvp-stabilization` FA sprint: D.SWEEP testing sweep RED→GREEN closeout, migration 0020 dedup-index `database.types.ts` header regen, D.E2E continuation rewiring. Next planned task was D.CODEX adversarial review (base `b6c15e0..HEAD`) but is parked for this out-of-band ask: fix the iOS calendar button bug (`bugs/bugsandimprovements.txt` item 33) on `components/dashboard/DashboardDateControl.tsx` plus iPhone/iPad component sweep.
