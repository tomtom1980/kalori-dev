- **[2026-05-01] Under `cacheComponents: false`, `revalidateTag` / `cacheTag` / `'use cache'` are forward-compat no-ops — `revalidatePath('/route', 'page')` is the load-bearing primitive for Router-Cache invalidation from a Route Handler.** `'use cache'` requires `cacheComponents: true` to function (build-fails otherwise); `cacheTag(...)` calls inside `unstable_cache` wrappers are silently ignored when no consumer subscribes via `cacheTag`. Keep the `revalidateTag` calls in mutation handlers as forward-compat for the eventual `cacheComponents:true` flip, but ADD `revalidatePath('/library', 'page')` (or equivalent) for the actual cache-invalidation contract under the current regime. Pairs with the `unstable_cache + cookies()` lesson: both require knowing the project's runtime cache mode before authoring fixes. (source: superpowers-exec-tomi · project: kalori)

- **React 19 `useOptimistic` state decays the instant the action resolves.** Card "fade out" flickers for ~200ms then reappears because optimistic value only holds DURING the pending transition; RSC `initial` prop still contains the row. Fix: call `router.refresh()` in the `onSuccess` callback (canonical), OR promote optimistic delta to real `useState`. Never assume optimistic state persists post-mutation.

- **[2026-05-07] When a server-side cache-invalidation primitive (`revalidatePath`) coexists with a client-side self-hydration codepath, E2E must PROVE the server primitive is load-bearing — not be silently propped up by client polling.** Pattern: attach `page.route('/api/<self-hydrate-route>', route => { calls.push(...); route.continue() })` listener BEFORE the user action. After the action + UI assertion lands, assert `calls.length === 0` (post-action UI state must come from the FIRST RSC payload, not a subsequent client fetch). If the listener fires non-zero, emit a console.warn NOTABLE — the server invalidation may be dead code masked by client redundancy. Applies to any "server `revalidatePath`/`revalidateTag` + client SWR/polling/tRPC subscription" combo where regression in the server primitive would silently degrade to client-only behavior. (source: superpowers-exec-tomi · project: kalori-mvp-stabilization)

- **[2026-05-15] Encoding/translation boundary fixes are symmetric — when fixing canonical→display drift on one side, audit display→canonical on the OTHER side in the same round.** Kalori C.1 Codex R1 caught canonical AI keys (`vitamin_c`) leaking into the existing `MicronutrientPanel` aggregator which keyed on display names (`Vitamin C`) — fix added `CANONICAL_CODE_TO_DISPLAY_NAME` lookup in `aggregate.ts`. Round 2 caught the SYMMETRIC gap: the new `resolveMicrosRda` resolver only read `micros[canonical]` and silently dropped legacy display-name keys from persisted/cached rows — fix added `DISPLAY_NAME_TO_CANONICAL_CODE` (inverse map, single source of truth in `lib/nutrition/micros-rda.ts`) and a key-resolution loop honoring both shapes. Discipline: when an encoding boundary is identified (canonical↔display, codepoint↔glyph, internal-unit↔external-unit, snake_case↔camelCase), fix BOTH directions in the same round and ship the inverse map alongside the forward map. Two pointers, two lookups, one source of truth. (source: superpowers-exec-tomi · project: kalori-mvp-stabilization)

- **[2026-05-15] Kalori canonical micro-key resolution uses a closed allowlist alias map `{code}_{unit} → code` auto-generated from `DEFAULT_MICROS_LIST` (`sodium_mg → sodium`) — cross-unit suffixes (`sodium_g`) drop silently rather than coerce.** `lib/dashboard/micros-rda-resolver.ts::canonicalizeMicroKey(rawKey)` is the SINGLE source-of-truth helper: resolution order alias → canonical → display-name → undefined. `aggregateMicros` calls it BEFORE the existing display-name translation. Why closed-allowlist + no implicit unit coercion: a `sodium_g` value (grams) silently auto-coerced to canonical `sodium` (mg) would 1000x the RDA contribution and produce wildly-wrong calorie/RDA-pct rollups. Drop-silently is the fail-safe — incorrect data is flagged by absence (RDA pct=0% jumps out), incorrect unit is invisible. Pattern: any code↔legacy key translation map for nutrition / measurement domains uses closed-allowlist semantics; unit-mismatch surfacing is intentional. Applies to future Kalori translation seams (macros, water units mL↔L, weight kg↔lb persistence). (source: superpowers-exec-tomi · project: kalori-mvp-stabilization)

- **[2026-05-17] When user reports "X isn't being saved/recorded", verify by reading the DB write path AND a sample of stored data BEFORE assuming the parse/save layer is broken — the bug may live downstream in the read/edit/display layer.** Kalori library-micros-parse batch: user reported "AI-parsed library items don't have micronutrients" implying the parse → save pipeline was dropping data. Investigation traced the full pipeline (prompt → schema → confirmation → /api/library/create → JSONB insert) and proved every layer was correct; the actual bug was that the edit-mode `EditMicrosCollapsible` only rendered inputs for 2 of 30 canonical micros (sugar + sodium gated on `saved > 0`), making the persisted iron / calcium / vitamins invisible in the edit dialog even though the view-mode rendered them correctly and the DB row carried them verbatim. Pattern at triage: when a user-symptom-statement implicates a write path, first run a one-line read (`select nutrition->'micros' from <table> where id=...` or equivalent) on a sample record to confirm the data IS or IS NOT in storage; only escalate to write-path investigation when the read confirms the data is missing. Saves a multi-hour parse-pipeline audit when the bug is a 3-line UI scope gap. Pairs with line 116 (Assert on downstream CONSUMER, not intermediate bookkeeping) — that's about test design; this is about triage prioritization. (source: bugfix-tomi · project: kalori)

- **[2026-05-17] When a renderer needs a new behavior branch (e.g. RDA-unknown rows should render with neutral treatment instead of misleading "0% low"), prefer Path A (extend the TypeScript enum + force `Record<Enum, T>` exhaustiveness check across every consumer) over Path B (sentinel value like `pct: null` with implicit branching).** Kalori library-micros-display-consistency R2 I2: Codex offered two paths to distinguish RDA-unknown dashboard rows from "actually low" rows. Path A — extend `MicroStatus` enum from 4-tuple (`'low'|'mid'|'good'|'over'`) to 5-tuple (add `'unknown'`); `microStatus()` returns `'unknown'` when `rda === null || rda === 0`. Path B — keep enum at 4 values but mark the row separately via `pct: null` + `if (pct === null) skipMeterAndLabel`. Path A wins because the `FILL_COLOR: Record<MicroStatus, string>`, `PCT_COLOR: Record<MicroStatus, string>`, `MICRO_TEXT_COLORS: Record<MicroStatus, string>`, and `statusWord` switch over `MicroStatus` all compile-fail at `tsc --noEmit` until every consumer adds an explicit `'unknown'` branch — exhaustiveness surface is the type checker, not human grep. Path B's sentinel survives partial consumer migration silently (any consumer that doesn't branch on `pct === null` just gets the old behavior with the wrong value). Apply: any time a discriminator needs to fan out across N renderers / palettes / aria-templates, lift it to a TypeScript union and gate consumers on exhaustive `Record<Union, T>` or exhaustive `switch` — let the compiler enumerate. (source: bugfix-tomi · project: kalori)

- **[2026-05-17] Defense-in-depth audits are LADDER patterns: when you add a server-side bound (e.g., MAX value clamp) to ONE mutation route, Codex will find every OTHER mutation route in the same vulnerability class that should also have it. Pre-emptively grep for all mutation routes against the same schema/table BEFORE declaring the bound "done."** Kalori library-micros-parse R1 added `MAX_MICRO_VALUE` clamp to `/api/library/[id]/update` only; Codex R2 surfaced 2 Criticals for the SAME class of missing bound on `/api/entries/save` (`ParsedItemSchema.micros`) and `/api/library/merge` (`NutritionSchema.micros`), forcing a Round-3 override to close both inline via a shared `lib/library/micros-bounds.ts` module. Pre-flight discipline: when adding a guard / clamp / validator to one route handler, immediately `grep -r "z.record.*number\|micros: z\." app/api/` (or equivalent for the data class) to enumerate sibling routes that need the same hardening. Apply the bound at all siblings in the same commit via a shared module — never per-route literal — so future routes inherit by import. Pairs with line 22 (Stale-state restore needs stop-aware gating for ALL fields) and line 193 (Sibling path symmetry) — both name the same pattern at different surfaces; this rule binds it to schema-layer guards across mutation routes specifically, with Codex's documented "find-the-other-N" tendency as the forcing function. (source: bugfix-tomi · project: kalori)

- **[2026-05-17] When a follow-up batch CLAIMS to preserve a prior-batch decision ("R1-C1 shape policy preserved"), Codex must verify the preservation was complete-not-partial — re-check the original commit's actual DIFF, not just the prior batch's commit MESSAGE.** Kalori library-micros-followups batch Bug 2: claim said "preserves R1-C1 shape policy" (the canonical/legacy preservation rule from a prior batch), but the prior batch's R1-C1 fix applied to `sodium` only and iron_mg / vitamin_c_mg / vitamin_d_mcg / calcium_mg / etc. were still being rewritten on unrelated macro edits. Codex R1 caught the scope gap; the fix only succeeded after extending preservation across ALL 30 canonical/legacy pairs (DEFAULT_MICROS_LIST enumeration + DISPLAY_NAME_TO_CANONICAL_CODE inverse map). Pattern: when verifying a "we preserved the prior decision" claim, the sub-agent's first action is to (a) `git show <prior-commit> -- <file>` to read the actual prior diff, NOT the commit message prose, (b) enumerate the symbols / cases the prior diff actually touched (e.g. `grep -n 'sodium' <prior-diff>` produces N matches, NOT 30 — that's the gap signal), (c) confirm the new batch's preservation covers the full set, not just the subset the prior batch addressed. (source: bugfix-tomi · project: kalori)

- **[2026-05-17] User clarifications given during ONE surface's discussion at the Phase-2 approval gate MUST be applied universally across ALL surfaces in the batch — sub-agents implementing the OTHER surfaces should NOT defensively "preserve historic behavior" on the un-discussed surfaces.** Kalori library-micros-display-consistency batch: user clarified at Phase 2 that RDA-unknown nutrients (sugar / caffeine) should ALWAYS SHOW sorted to END of the list; clarification was given while discussing Surface C (Library FoodDetail). Phase 3 sub-agent implementing Surface A (Dashboard MicronutrientPanel) chose `includeUnknownRda: false` with an inline comment "preserves historical dashboard behavior" — Codex R1 Critical C1 caught this as a direct user-intent violation and called it out by name: the historical behavior IS what the user clarification was overriding, and breaking cross-surface consistency was the entire premise of the batch. Discipline at sub-agent-briefing time: when a Phase 2 clarification governs the cross-surface contract, the briefing for every surface's sub-agent must re-state the clarification VERBATIM and forbid the "preserve historic" carve-out. Codex's adversarial framing reliably catches this; the defensive-historic pattern reads to Codex as a deliberate override of user intent, not as conservative engineering. (source: bugfix-tomi · project: kalori)
