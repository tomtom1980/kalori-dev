# Approval Gate: 2026-05-18-1328-calorie-tracker-fixes

| Bug | Classification | Root cause | Proposed change | Files | TDD | UI | Risk | Stop flags / open decisions |
|---:|---|---|---|---|---|---|---|---|
| 1 | needs_debug_shallow | Whole-style unit heuristic is partial and client/server validation still accepts decimals. | Add shared whole-style unit helpers; enforce integer-only behavior across log, library edit, and API schemas. | 10: `portion-unit.ts`, `ConfirmationScreen.tsx`, `LibraryList.tsx`, `useFoodDetailEdit.ts`, `FoodDetailName.tsx`, `entries/save/route.ts`, `create-schema.ts`, `foodDetail.schema.ts`, `library/[id]/update/route.ts`, `library/merge/route.ts` | yes | yes | medium | STOP: touches >5 files. Decision: `cup`/`cups` integer-only everywhere. Open: reject vs snap decimals; migrate old rows? |
| 2 | known_fix | TimeEditor UI max allows five future minutes, mirroring server skew tolerance. | Set UI max to current time, guard future changes, and show specific future-time save error. | 4: `TimeEditor.tsx`, `ConfirmationScreen.tsx`, `en.ts`, `ConfirmationScreen.test.tsx` | yes | yes | low | No blocking decisions. Preserve server 5-minute skew tolerance. |
| 3 | known_fix | AI details component receives reasoning only, not parsed item micronutrients. | Pass parsed items into details; aggregate micros; show top RDA percent and expandable remaining rows. | 5: `WhyTheseNumbers.tsx`, `ConfirmationScreen.tsx`, `en.ts`, `WhyTheseNumbers.test.tsx`, maybe `ConfirmationScreen.test.tsx` | yes | yes | medium | No stop flag. Open: photo reasoning support is adjacent, not required. |
| 4 | known_fix | Library re-log mapper scales macros but hard-codes parsed item micros to `{}`. | Carry library micros through hydration and scale them in library selection confirmation payloads. | 5: `useLogFlowStore.ts`, `to-log-library-item.ts`, `LibraryList.tsx`, `to-log-library-item.test.ts`, `library-tab-continue-cta.test.tsx` | yes | yes | low | No blocking decisions. |
| 5 | actually_a_feature | Existing parse/library contracts do not store approximate gram equivalents for whole-style units. | Re-route as feature: define gram-equivalent source, persist if needed, then render subtle secondary text. | 9: `schemas.ts`, `portion-sanity.ts`, `ConfirmationScreen.tsx`, `create-schema.ts`, `fetch.ts`, `LibraryCard.tsx`, `FoodDetailName.tsx`, `foodDetail.format.ts`, `globals.css` | yes | yes | medium | STOP: feature, >5 files. Decision: only newly recorded items going forward; no historical/library retrofit. Need gram source decision. |
| 6 | known_fix | Food Detail edit dropdown includes egg-specific units in local `UNIT_OPTIONS`. | Remove egg-specific options from default dropdown and update stale component test expectations. | 2: `FoodDetailName.tsx`, `FoodDetail.mode-edit-query.test.tsx` | yes | yes | low | Open: preserve legacy selected egg-specific values or coerce them? Smallest fix preserves. |
| 7 | known_fix | Progress unit switch controls quick-add only; chart ignores `unitPref` and renders kg. | Move unit state to progress panel and convert chart values, goal, ticks, labels, and quick-add together. | 5: `progress/page.tsx`, `weight-quick-add.tsx`, `WeightTrajectoryLine.tsx`, `WeightTrajectoryLine.test.tsx`, `weight-quick-add.test.tsx` | yes | yes | medium | No blocking decisions. Persisted records remain kg-canonical. |
| 8 | known_fix | Image AI quota counts library items, not shared vision and sketch-generation AI calls. | Add shared image-AI quota over `ai_call_log`; enforce in vision and sketch generation. | 5: `cost-log.ts`, new migration, `image-usage-quota.ts`, `ai/vision/route.ts`, `sketch-pipeline.ts` | yes | no | medium | Open: should cache hits count? Failed generation counts proposed. Keep `/api/library/quota` name? |
| 9 | actually_a_feature | Progress heatmap intentionally aggregates five fixed micronutrients, not all ranked minor elements. | Scoped enhancement: aggregate canonical micros, rank default four under-target rows, and add expandable detailed table/tooltips. | 6: `progress.ts`, `micros-rda.ts`, `MicronutrientHeatmap.tsx`, `HeatmapInteractive.tsx`, `progress.test.ts`, `MicronutrientHeatmap.test.tsx` | yes | yes | medium | STOP: feature, >5 files. Decision: keep in batch; use `DEFAULT_MICROS_LIST`; exclude zero/<1% DV; sodium excluded from top-four but included expanded. |

## Missing or Malformed Proposals

None found. All `bug-1.md` through `bug-9.md` were readable and contained the needed proposal sections. `state.md` frontmatter contains duplicate `last_completed_action` and `last_user_decision` keys; both decision strings were preserved while phase metadata was updated.

## Additional User Decisions

- Bug #5 source of truth: for new AI/image-parsed items with non-gram serving units, AI should return an `approxGrams` value, and the app should persist/display that metadata. Do not use a hard-coded local conversion table for food-specific units.
- Bug #6 legacy egg unit behavior: preserve and display existing saved egg-specific unit values, but remove egg-specific units from dropdown options for new edits. If the user changes the unit, they must choose from the cleaned list.
- Bug #8 image AI quota behavior: only real AI model calls count against the shared daily/monthly image quota; cache hits and reused results may be logged as telemetry but must not consume quota.
- Bug #8 naming/copy: user-facing quota copy is `AI image analysis limit`; internal names should align to image analysis, for example `image_analysis_daily_limit` / `image_analysis_monthly_limit`, rather than generic image AI or library quota naming.
