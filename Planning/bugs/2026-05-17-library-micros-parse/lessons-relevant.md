- **[2026-05-17] Mocking the HTTP client bypasses the shared validation schema — green tests can mask a production 400.** When the prod route runs request bodies through a shared Zod (or equivalent) schema (e.g. `CreateLibraryBodySchema`) but unit tests mock the client (`authFetch`, `fetch`, axios) at the call boundary, the asserted body shape never traverses the validator. A contract violation (UUID-shaped field receiving `${clientId}:${idx}` suffix that's no longer a UUID) ships green. Defense-in-depth: pair client-mock tests with EITHER (a) a unit test that runs a representative emitted body through the same shared schema (`expect(CreateLibraryBodySchema.safeParse(body).success).toBe(true)`) OR (b) a thin integration test against the actual route handler. Distinct from line 41 (dead-helper tests) and line 140 (mock-writes-always-succeed): this is one layer higher — the test never exercises the shared validator that the production path enforces. (source: superpowers-exec-tomi · project: kalori-mvp-stabilization)

- **[2026-05-14] Encoding/translation boundary fixes are symmetric — when fixing canonical→display drift on one side, audit display→canonical on the OTHER side in the same round.** Kalori C.1 Codex R1 caught canonical AI keys (`vitamin_c`) leaking into the existing `MicronutrientPanel` aggregator which keyed on display names (`Vitamin C`) — fix added `CANONICAL_CODE_TO_DISPLAY_NAME` lookup in `aggregate.ts`. Round 2 caught the SYMMETRIC gap: the new `resolveMicrosRda` resolver only read `micros[canonical]` and silently dropped legacy display-name keys from persisted/cached rows — fix added `DISPLAY_NAME_TO_CANONICAL_CODE` (inverse map, single source of truth in `lib/nutrition/micros-rda.ts`) and a key-resolution loop honoring both shapes. Discipline: when an encoding boundary is identified (canonical↔display, codepoint↔glyph, internal-unit↔external-unit, snake_case↔camelCase), fix BOTH directions in the same round and ship the inverse map alongside the forward map. Two pointers, two lookups, one source of truth. Pairs with line 146 (sibling path symmetry: grep all callsites) — that's about producer-side symmetry; this is about producer-vs-consumer symmetry across a translation seam. Applies to any encode/decode pair (i18n key↔display, normalized↔raw, internal↔wire). (source: superpowers-exec-tomi · project: kalori-mvp-stabilization)

- **[2026-05-15] Kalori canonical micro-key resolution uses a closed allowlist alias map `{code}_{unit} → code` auto-generated from `DEFAULT_MICROS_LIST` (`sodium_mg → sodium`) — cross-unit suffixes (`sodium_g`) drop silently rather than coerce.** `lib/dashboard/micros-rda-resolver.ts::canonicalizeMicroKey(rawKey)` is the SINGLE source-of-truth helper: resolution order alias → canonical → display-name → undefined. `aggregateMicros` calls it BEFORE the existing display-name translation. Why closed-allowlist + no implicit unit coercion: a `sodium_g` value (grams) silently auto-coerced to canonical `sodium` (mg) would 1000x the RDA contribution and produce wildly-wrong calorie/RDA-pct rollups. Drop-silently is the fail-safe — incorrect data is flagged by absence (RDA pct=0% jumps out), incorrect unit is invisible. Pattern: any code↔legacy key translation map for nutrition / measurement domains uses closed-allowlist semantics; unit-mismatch surfacing is intentional. Applies to future Kalori translation seams (macros, water units mL↔L, weight kg↔lb persistence). Pairs with line 82 (Core Principle: symmetric encoding/translation boundary). (source: superpowers-exec-tomi · project: kalori-mvp-stabilization)

- **Partial edits to JSONB columns must carry full post-edit shape, not a diff.** Shallow `.update({ nutrition: { macros: { protein_g: X } } })` on a JSONB column SILENTLY NULLS every sibling (carbs/fat/fiber/micros/kcal). Client computes full post-edit object; server Zod requires all sibling keys + `.strict()` rejects partial bodies with 400 so future regressions surface loudly. Applies to any shallow-merge column (JSONB, document-store fields, Prisma `Json` type).

- **Test scaffolds that mock writes to ALWAYS succeed (`error: null`) make the error-path invisible to TDD.** A `buildMocks()` helper that hard-codes `food_library_items.insert(...).single()` to `{ data: row, error: null }` keeps 1735/1735 GREEN while production fires `revalidateTag` + `revalidatePath` on PostgREST 23505 / RLS denial / schema drift. Codex's adversarial pass reads error-path branches that mocked TDD doesn't exercise. Pattern: when adding a test for any write that uses a shared mock-builder, ALSO add a sibling test that overrides the mock to return `{ data: null, error: { code, message } }` and asserts side-effects (revalidate, audit, downstream writes) DID NOT fire. Pairs with the existing PostgREST `.maybeSingle()` lesson but applies at TEST scaffold level, not callsite level. (source: superpowers-exec-tomi · project: kalori)

- **Schema drift is invisible to mocked-unit tests.** Only real-PostgREST integration tests or `supabase gen types` compile-time guard catch column/table renames. Two defenses: (a) `supabase gen types typescript > types/supabase.ts` in CI with diff-check; (b) real-PostgREST integration tests for every table in production reader paths.

- **[2026-05-17] Codex Critical findings on a `bugfix-tomi` batch require an A/B determination: was the issue INTRODUCED by THIS batch, or PRE-EXISTING in code the batch only touched in passing? Record the determination with git-diff evidence, NOT just intuition.** Kalori library-micros batch R1 C2: Codex flagged a Critical retry-safety hazard on `ConfirmationScreen.tsx` lines 751-814 (sequential POST loop + fresh-UUID-per-attempt + row[0]-only pre-flight dedup). Investigation ran `git log -- ConfirmationScreen.tsx`, `git diff <pre-batch>..<post-batch> --stat -- ConfirmationScreen.tsx` (`+133 -0`, all additive in `EDIT_ITEM_MICRO` / `ConfirmationItemMicros`), and grep against `planning/followups.md` — proved the hazard pre-existed at baseline `60e85c5` and was tracked at HIGH severity as `POST-MVP-CODEX-R3-C1` + `R3-C2` from a prior batch. Action: file the determination as a written artifact (`fixes-r1-<file>.md`) with the git-diff evidence quoted verbatim, update the existing followup with the re-surfacing + sharpened analysis, and DO NOT auto-fix in this batch. Anti-pattern: silently expanding scope to "while we're here, let's also fix this Critical" — that violates the user-approved batch contract and grows commit drift. Distinct from line 17 (pre-existing defects elevate to HIGH when new exposure surface expands — that triggers in-batch fix) because here the batch's diff did NOT change the surface in question (additive-only, didn't touch the save loop). Pairs with line 191 (pre-existing arch gap = force-commit + followup) — same family applied at the per-Codex-finding boundary inside a bug-batch. (source: bugfix-tomi · project: kalori)

- **[2026-05-17] Commit-per-bug + push-immediately is the survival pattern when a concurrent Claude Code session can `git stash; git reset --hard` your working tree mid-batch. NEVER batch all bug commits at Phase 8 — each bug commits and pushes the moment it lands GREEN, locking the work into `origin/main` before the next stash cycle.** Kalori library-micros batch: mid-implementation a sibling Claude Code session (different `bugfix-tomi` batch — bottom-tab-bar / portion-unit) ran `git stash; git reset --hard` on this working tree (stash labeled `concurrent-session WIP isolation for E.CODEX R2 push`). Bug 2 + Bug 3 source + test work survived via `git checkout stash@{0} -- <paths>` because they were captured in the stash. Bug 1's production code on `ConfirmationScreen.tsx` was NOT in stash@{0} (the sibling session didn't stage it) — the test file survived as TDD anchor but the production implementation was wiped. Re-implementation cost: a full Bug 1 re-spawn against the recovered test file (4/4 GREEN), then commit `45376f8` + push-unblock side-fix `9361fe6`, then push. Two side-effects worth surfacing: (a) a pre-push typecheck hook flagged a pre-existing TS error from a sibling commit (`b51cad1`) — applied a precedent surgical type-widening cast (`a0879b1` pattern) as a side-commit so the Bug 1 push wasn't blocked through ANOTHER stash window; (b) `lint-staged` wraps every commit in its own stash that unwinds cleanly — visible in `git stash list` during audit but non-destructive. Discipline: commit each bug's production + test diff as soon as the affected-module sweep is GREEN, push immediately, treat `pre-push hook fails on sibling-introduced error` as a side-fix worth shipping in scope (with explicit logging) to keep the push window short. Pairs with the user-memory note "commit fast on concurrent sessions" — this is the bugfix-tomi enforcement of that pattern at every per-bug boundary, not just at Phase 8. (source: bugfix-tomi · project: kalori)

- **[2026-05-17] Phase 8 commit in bugfix-tomi MUST stage explicit paths from `state.md::bugs[].files_touched` (plus state.md + e2e-results.md + lessonlearned.md + manifest.md + CHANGELOG.md), never `git add -A` or `git add .` — in repos with concurrent Claude Code sessions, the working tree at Phase 8 routinely holds unrelated sibling-batch modifications that would contaminate the batch's commit if added blanket.** Kalori mobile-bottom-nav batch's Phase 7 sweep advanced HEAD from `dda828e` to `e7400e9` because a concurrent session pushed 3 commits during the visual-baseline refresh window, leaving unrelated FoodDetailMacros / ConfirmationScreen / micros-rda-resolver / i18n drift in the working tree at Phase 8 — `git add -A` would have rolled all of it into the bugfix's commit and broken Codex audit scope. Discipline: at Phase 8, iterate `state.md::bugs[].files_touched` (already enumerates the canonical per-bug surface from Phase 3 implementation tracking) + add the batch-tracking artifacts explicitly; immediately post-`git add`, re-run `git status --short` and verify the unrelated modifications are STILL showing as unstaged (proves the staging set excluded them). Pairs with line 195 (surgical staging via `git add <path>` per file when working-tree carries pre-existing dirty drift) — that rule applies at every task commit; this rule binds it to the bugfix-tomi Phase 8 commit step specifically, with the state.md `files_touched` field as the canonical staging-list source. Applies to any multi-phase batch orchestrator where the working tree can drift between Phase 3 and Phase 8. (source: bugfix-tomi · project: kalori)

- **[2026-05-14] Kalori resolver pattern for DT-5/O-2 deferred per-user override columns: signature accepts ONLY the runtime entry set, NO `profile` parameter — type-system makes accidental override-column consumption a compile error until the followup widens the signature.** `lib/dashboard/micros-rda-resolver.ts::resolveMicrosRda(todayEntries: FoodEntry[]) => MicroRdaRow[]` is the canonical shape: reads `DEFAULT_MICROS_LIST` only, sums per-item micros across entries, computes pct + threshold. When `F-MICROS-RDA-OVERRIDE-COLUMN` lands, the signature widens additively to `(todayEntries, profile?: { micros_rda_override?: ... })` — every existing call site still type-checks; the override-consumption is the explicit new code path. Same shape applies to future Kalori deferred-override resolvers (calorie target override, macro split override, water-cap override): omit the profile param entirely from the signature rather than reading it with a TODO comment. Pairs with Core Principle "Type signatures are the highest-fidelity deferral enforcement" — this is the Kalori-specific binding. (source: superpowers-exec-tomi · project: kalori-mvp-stabilization)
